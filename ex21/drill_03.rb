puts "here is an example"

first = add(age, subtract(height, multiply(weight, divide(iq, 2))))

puts "This becomes : #{first}. This can be done in another way"

second = add(age, subtract(height, multiply(weight, divide(iq, 2))
puts  "changing the order of the arguemnts entered into subtract, you get #{second}"
